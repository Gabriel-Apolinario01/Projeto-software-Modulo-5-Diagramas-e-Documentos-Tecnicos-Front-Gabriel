{
  "name": "docula-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "Frontend do modulo de diagramas da plataforma DocuIA.",
  "scripts": {
    "start": "node server.js",
    "build": "node --check src/app.js && node --check src/config.js && node --check server.js",
    "test": "node --check src/app.js && node --check src/config.js && node --check server.js"
  }
}
